/* Retorna um único elemento com um ID especifico.*/
const = document.getElementById('titulo');

/* Retorna uma coleção de elementos que possuem uma classe especifica.*/
const = document.getElementsByClassName('paragrafo');

/** Retorna todos os elementos de um tipo específico (tag) no documento.*/
const  = document.getElementsByTagName('p');
/*
* Retorna o primeiro elemento que corresponde a um seletor CSS.
* Utiliza os mesmos padrões dos seletores CSS 
*/
const = document.querySelector('.paragrafo');

/*
* Retorna todos os elementos que correspondem ao seletor CSS 
* Utiliza os mesmos padrões dos seletores CSS
*/

const = document.querySelectorAll('#titulo');
